import { Routes } from '@angular/router';
import { IncidentsComponent } from './incidents.component';

export const incidentRoutes: Routes = [
    { path: 'incidencias', component: IncidentsComponent }
];
